# Stockage local

Une fermeture autorisée est mémorisée dans le `localStorage` du navigateur. La clé combine l’instance du Web Part et une signature du contenu. Le message réapparaît donc lorsqu’il est modifié. Aucune donnée personnelle n’est transmise ou stockée dans SharePoint.
